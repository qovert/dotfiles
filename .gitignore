# Shell/history files -- never commit real history, it leaks hostnames,
# usernames, and occasionally credentials typed on the command line.
*_history
.zsh_history
.bash_history
.pwsh_history
.python_history
history.txt
